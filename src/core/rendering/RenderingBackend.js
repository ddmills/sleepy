export default class RenderingBackend {
    setSize(width, height) {
    }

    draw(x, y, sprite, fg1, fg2, bg) {
    }

    clear() {
    }

    clearArea(x, y, width, height) {
    }

    render() {
    }
}
