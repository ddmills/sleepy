export default class Status {
    isDot = false;

    constructor(key, name, type, glyph) {
        this.key = key;
        this.name = name;
        this.type = type;
        this.glyph = glyph;
    }
}
