import { Component } from 'geotic';

export class Glyph extends Component {
    static properties = {
        primary: '#ddd',
        secondary: '#666',
        background: null,
        char: '?',
    };
}
